1.create git repo and clone it in local
2.create mycode.py and add code to it and it create the nwe csv file in the folder location path.
3.Do a add-git-commit-push command before initializing the  dvc. 
4.run pip install dvc in terminal 
5.now we do dvc init(creates .dvcignore .dvc) 
6.now create "mkdir s3"(creates directory of s3) 
7.now we do "dvc remote add -d myremote S3" 
8.Next "dvc add data/"
  now it will ask(git rm -r --cached data and git commit -m "stop tracking data") 
  because initially we are tarcaking data/ folder for git so now now we remove it for dvc to handle.
9.Again we do "dvc add data/"(creates data.dvc) and then "git add .gitignore data.dvc".
10.now we do dvc commit and dvc push.