create git repo and clone it in local
create mycode.py and add code to it and it create the nwe csv file in the folder location path.
Do a add-git-commit-push command before initializing the  dvc. 
# run pip install dvc in terminal 
now we do dvc init(creates .dvcignore .dvc) 
now create "mkdir s3"(creates directory of s3) 
now we do "dvc remote add -d myremote S3"